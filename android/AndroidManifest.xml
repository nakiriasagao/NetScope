<?xml version="1.0" encoding="utf-8"?>
<!--
  NetScope Android 客户端（独立运行版）

  关键点：后端服务跑在手机内部（core/NetHttpd + core/NetApi），
  探测逻辑全部由 core/Probe.java 在本机执行，**不依赖电脑**。

  权限说明：
    · INTERNET              WebView 加载本机 127.0.0.1 服务；顺带访问地理定位接口
    · ACCESS_NETWORK_STATE  判断联网状态，给出更准确的提示
  注意：本应用**不申请**定位 / 存储 / 相机 / 电话等敏感权限。
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.netscope.app"
    android:versionCode="2"
    android:versionName="1.1.0">

    <uses-sdk
        android:minSdkVersion="21"
        android:targetSdkVersion="34" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".NetScopeApp"
        android:label="@string/app_name"
        android:icon="@drawable/ic_launcher"
        android:roundIcon="@drawable/ic_launcher"
        android:theme="@style/AppTheme"
        android:hardwareAccelerated="true"
        android:usesCleartextTraffic="true"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:launchMode="singleTop"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
