# 更新记录

## 2025-09-17（第十二次修订）· 地图地区划分 + 局域网锁定世界地图 + 增量测试

### 新增：内置世界地图显示「国家内地区划分」

从 Natural Earth 1:50m 的 `admin_1_states_provinces_lines` 提取一级行政区
（省 / 州 / 地区）边界，在构建期完成投影与日界线切分，与国界使用同一套变换：

- 数据：`public/data/world-110m.json` 新增 `provinces` 数组（536 条线 / 16978 个顶点）；
- 每条线带包围盒 `{ p: [...], b: [...] }`，前端据此做视口裁剪 ——
  实测放大到 9.3 倍时单帧绘制仅 **0.04 ms**；
- **按缩放层级淡入**：全球视图（缩放 < 1.4）只显示国界，避免线条糊成一片分不清国家；
  1.4→2.4 线性渐显，放大后清晰呈现各省/州边界；
- 新增「显示国家内地区划分」开关（默认开启）；
- 构建脚本：`node tools/build-world-map.js`（默认 50m，可用 `--admin1=110m` 或 `--no-admin1`）。

### 新增：扫描局域网后锁定世界地图

局域网设备使用私有地址、没有地理坐标，画到世界地图上没有意义。现在的行为：

1. 点击「扫描局域网设备」→ 自动切到逻辑拓扑、**禁用「世界地图」按钮**；
2. 此时强行点世界地图无效（并给出说明），切换底图也仍保持星型拓扑；
3. 用户**下一次探测外网**后自动解锁，世界地图恢复可用。

### 修复：切换底图会覆盖局域网星型拓扑

`drawCurrentTrace()` 用 `renderer.lanMode` 判断是否该画星型拓扑，
而挂起高德 / 切换底图时该标志会被重置，导致星型拓扑被探测逻辑拓扑覆盖。

**修复**：改用语义化判断 —— 只要有局域网拓扑数据且处于锁定（或星型模式），
就优先绘制星型拓扑，不再依赖会被重置的内部标志。

### 新增：增量测试入口（全量测试太慢）

`test/run-tests.js` 重写为可选的测试注册表：

```powershell
node test/run-tests.js --only=unit            # 只跑单元测试（0.2 秒）
node test/run-tests.js --only=unit,admin1,lan # 只跑与本次改动相关的
node test/run-tests.js --list                 # 列出全部可用测试名与分组
node test/run-tests.js --all                  # 全量（发布前）
node test/run-tests.js                        # 默认：unit + smoke
```

分组：`core`（unit/smoke）、`map`（全部地图/底图/拓扑类用例）。

### 测试规模

当前 15 项：单元 101 用例、接口冒烟 12 项、以及 13 项浏览器验收
（局域网拓扑、节点着色、世界地图地区划分、高德底图、底图切换、图例与样式一致性、
局域网扫描后再探测、视图切换、两种拓扑性能、探测外网逻辑拓扑、高德逻辑拓扑、
切底图保持视图、局域网锁定世界地图）。

---

## 2025-09-17（第十一次修订）· 修复「逻辑拓扑下切换底图会跳回世界地图」

用户反馈：在逻辑拓扑模式下切换高德 / 内置世界地图，画面会直接变成世界地图，
但上方按钮仍显示「逻辑拓扑」；并且切换会导致动画暂停。

### 根因：底图切换逻辑没有考虑当前视图

`switchBaseMap()` 的两条分支都只关心"底图怎么画"，没检查当前是哪个视图：

- **切到内置地图**：`drawCurrentTrace()` 内部按 `state.view` 绘制，
  但在此之前调用了 `renderer.setMode('map')`/`resize()` 的路径把渲染器切成了地图模式；
  同时 `amapView.destroy()` 会 `stopAnimation()`，切完没有重新启动。
- **切到高德**：直接 `view.setTrace(...)` 交给高德绘制（高德只画地理视图），
  于是画面变成世界地图，而 `state.view` 仍是 `graph`、按钮也仍是「逻辑拓扑」
  —— 画面与 UI 状态不一致。

### 修复：让底图切换尊重当前视图

| 当前视图 | 切到高德 | 切到内置地图 |
| --- | --- | --- |
| 世界地图 | 高德接管，画在叠加层 | 内置引擎画地图 |
| **逻辑拓扑** | **立即挂起高德**，数据仍由内置引擎按跳展开绘制 | **保持逻辑拓扑**（`setMode('graph')` + 按跳展开） |

并且在两条分支结束后都按 `renderer.options.animate` 重新 `startAnimation()`，
避免切换底图后动画停在原地。高德加载失败的回退分支同样处理。

实测（探测 8.8.8.8，11 个已定位跳）：

| 操作 | state.view | 按钮 | 渲染模式 | 节点 | 动画 |
| --- | --- | --- | --- | --- | --- |
| 基线 内置 + 逻辑拓扑 | graph | graph | graph | 11 | 运行 |
| 逻辑拓扑 → 切高德 | graph | graph | graph | 11 | 运行 |
| 逻辑拓扑 → 切回内置 | graph | graph | graph | 11 | 运行 |
| 世界地图 → 切高德 | map | map | map | 4 | 运行 |

### 顺带更新两处过时断言

`test/lan-then-trace-e2e.js` 与 `test/graph-trace-e2e.js` 里原先把
"切底图 ⇒ 渲染模式变成 map / 高德启用叠加绘制"当作正确行为，
那正是本次要修掉的问题。已改为断言**视图保持不变**，
并补上"显式切到世界地图后高德才接管"的步骤。

### 新增测试

| 文件 | 作用 |
| --- | --- |
| `test/basemap-keep-view-e2e.js` | 逻辑拓扑 / 世界地图两种视图下切换底图：校验 `state.view`、视图按钮、渲染模式、渲染画布、节点数、着墨量与**动画是否仍在运行** |

测试入口 `test/run-tests.js` 在 `NS_WITH_BROWSER=1` 下一共运行 13 项，当前全部通过。

---

## 2025-09-17（第十次修订）· 修复「高德地图模式下逻辑拓扑完全不显示」

用户反馈：高德地图模式下的逻辑拓扑完全不显示。

实测复现（探测 8.8.8.8 → 切高德 → 点「逻辑拓扑」）：

```
节点=14 连线=13 标签=14        ← 数据其实都算好了
内置画布: {"hidden":true, "display":"none", "size":[0,0]}   ← 但画布是隐藏的！
```

这是**三个叠加的缺陷**，任一都会导致"看不到"。

### 缺陷 1：挂起高德后没有让内置画布重新可见

高德模式下内置画布是 `hidden` + `display:none`（尺寸为 0×0）。
`AmapView.suspend()` 只把渲染目标交还内置画布，**没有取消隐藏、也没有重新量尺寸**，
于是数据画进了画布却什么都看不见。

**修复**：挂起时显式显示内置画布、重新获取 2D 上下文并 `resize()`（隐藏元素量不到尺寸）；
恢复时反向操作（隐藏内置画布、把目标交还叠加层）。

### 缺陷 2：`drawCurrentTrace()` 把数据又交给了已挂起的高德

调用链实测：

```
merge=false hops=15  4→14    ← 视图按钮正确按跳展开
merge=true  hops=15 14→4     ← drawCurrentTrace 经 amapView.setTrace 又合并回去
```

`drawCurrentTrace()` 只判断 `if (amapView)`，而高德此刻已被挂起、应由内置渲染器接管。
高德分支按地理坐标合并，把"按跳展开"的结果覆盖成了 4 个点。

**修复**：改用 `amapView.isSuspended()` 判断——挂起期间不再把数据交给高德。
同时把视图按钮里的两处 `renderer.setTrace(...)` 也收敛到 `drawCurrentTrace()`，
彻底消除"同一份数据在两处以不同合并规则绘制"的可能。

### 缺陷 3：`AmapView.setTrace()` 无条件按坐标合并

`AmapView.setTrace()` 内部调用 `renderer.setTrace(hops, context)`，未传 `merge`（默认合并）。
即使数据已经按跳展开，只要再经过一次高德重绘就会被合并掉。

**修复**：按渲染器当前模式决定合并规则（`graph` 模式不合并）。

### 顺带修复：逻辑拓扑视图下动画不启动

`setSuspended` 会 `stopAnimation()`，切到逻辑拓扑后没有重新启动，
逻辑拓扑变成静止画面。**修复**：切换后按 `options.animate` 重新启动。

### 实测结果

| 步骤 | 结果 |
| --- | --- |
| 高德 + 世界地图 | 4 节点（按坐标合并），画布 `overlay-canvas`，可见 |
| 高德 + 逻辑拓扑 | **14 节点 / 13 连线**，画布 `map-canvas`，`display:block` 992×839，动画运行 |
| 切回世界地图 | 恢复高德底图，4 节点，画布 `overlay-canvas` |

### 新增测试

| 文件 | 作用 |
| --- | --- |
| `test/amap-graph-e2e.js` | 高德底图下逻辑拓扑验收：渲染目标可见性（hidden / display / 尺寸 / 上下文匹配）+ 按跳展开断言，覆盖"先探测再切高德"与"先切高德再探测"两条路径 |

测试入口 `test/run-tests.js` 在 `NS_WITH_BROWSER=1` 下一共运行 12 项，当前全部通过。

---

## 2025-09-17（第九次修订）· 修复「探测外网的逻辑拓扑显示错误」

用户反馈：局域网的逻辑拓扑正常了，但探测外网的逻辑拓扑仍然显示错误。

### 根因：`resetForNewRun()` 强行把视图改回了世界地图

上一轮为了修「扫描局域网后再探测看不到世界地图」，在 `resetForNewRun()` 里加了：

```js
renderer.lanMode = false;
state.lan = null;
state.view = 'map';                       // ← 问题在这里
document.querySelectorAll('[data-view]')  //   还把视图按钮也切成了「世界地图」
  .forEach(b => b.classList.toggle('is-active', b.getAttribute('data-view') === 'map'));
```

于是**用户在探测前选择的「逻辑拓扑」被强行改成了「世界地图」**：

- 世界地图视图按地理坐标合并节点（同城多跳只显示一个标记）；
- 结果"逻辑拓扑视图下探测外网"得到的仍是合并后的 4 个节点，
  看起来就像逻辑拓扑显示错误。

实测对比（探测 8.8.8.8，14 跳 / 11 个已定位）：

| | 修复前 | 修复后 |
| --- | --- | --- |
| `state.view` | `map`（被改掉） | `graph`（保持用户选择） |
| 逻辑拓扑节点 | 4（按坐标合并） | **12**（本机 + 11 个已定位跳） |
| 连线 | 3 | **11** |

**修复**：`resetForNewRun()` 不再改动视图与视图按钮，
只退出局域网星型模式（`renderer.lanMode = false`）。
「探测后却看到星型拓扑」这一原始问题由视图切换处更精确的判断解决
（只有"确实在展示星型拓扑"时才保留该模式）。

### 顺带修复：增量重绘漏传 `merge`，边收边画时拓扑会反复塌陷

追踪过程中每收到一批跳点都会重绘，但增量路径调用的是：

```js
renderer.setTrace(state.hops, { local, target });   // 没有 merge，又按坐标合并
```

导致探测期间逻辑拓扑反复塌成"本机 → 目标"一条线，直到完成才恢复。

**修复**：把散落在 5 处的重绘逻辑收敛成一个 `drawCurrentTrace()` 统一入口，
合并规则只在这一处决定（地图视图合并 / 逻辑拓扑按跳展开），
增量刷新用 `{ fit: false }` 避免画面每来一批跳点就跳动。

### 新增测试

| 文件 | 作用 |
| --- | --- |
| `test/graph-trace-e2e.js` | 探测外网时的逻辑拓扑三段验收：①逻辑拓扑视图下探测（含边收边画的增量阶段）②世界地图 ⇄ 逻辑拓扑往返 ③高德底图下的逻辑拓扑与底图选择保持 |

测试入口 `test/run-tests.js` 在 `NS_WITH_BROWSER=1` 下一共运行 11 项，当前全部通过。

---

## 2025-09-17（第八次修订）· 逻辑拓扑按跳展开、标签层空白、星型拓扑动画

用户反馈：动画卡顿的是**局域网星型拓扑**；「逻辑拓扑」选择后无法显示；星型拓扑仍然卡顿。

### 修复 1：逻辑拓扑把"同城多跳"合并成 1 个点 → 看起来像没显示（主因）

**根因**：`setTrace()` 会按地理坐标合并节点（相同 `lat/lon` 只保留一个），
这在世界地图上是必要的（避免同城多跳重叠成一堆标记），
但用在**逻辑拓扑**上就出问题了：

```
实测：13 跳里 ttl 1~6 的 geo 全是 Shanghai（同一个城市坐标）
      → 被合并成 1 个节点
      结果逻辑拓扑只有 2 个节点、1 条连线（本机 → 目标）
```

用户看到的"逻辑拓扑无法显示"，实际是"只显示了一条线"。

**修复**：`setTrace()` 新增 `merge` 选项（默认 `true` 保持地图行为）；
逻辑拓扑视图传入 `merge: false`，**每一跳各成一个节点**，
同城节点自动加序号区分（`Shanghai`、`Shanghai #2` …）。

实测效果（上海 → 杭州，13 跳）：

| | 修复前 | 修复后 |
| --- | --- | --- |
| 逻辑拓扑节点 | 2 | **8**（本机 + 7 个已定位跳） |
| 连线 | 1 | **7** |

### 修复 2：节点较多时 SVG 标签层永久空白

**根因**：`drawOverlay()` 第一行就清空整个 SVG 图层，
随后 `showAll = nodes.length <= 8` —— 按跳展开后节点变多，该值为 `false`；
若当时没有悬停或选中节点，函数直接返回，**清空后没有再写回任何内容**，
标签层就永久空白了。

**修复**：改为「均匀抽样 + 必显 + 避让」策略：

1. 节点数超过上限（地图 14 个 / 星型拓扑 24 个）时按 `stride` 均匀抽样，
   保证画面上始终有可读标签；
2. **起点、目标**以及悬停 / 选中的节点始终显示；
3. 逐个尝试上/下偏移避让，实在放不下才跳过（宁可少显示，也不叠成一团）。

### 修复 3：星型拓扑没有动画、逻辑拓扑动画不启动

**根因**：`setLanTopology()` 里调用了 `stopAnimation()`，
而 `setOptions()` 只在 `mode === 'map'` 时启动动画 ——
于是星型拓扑画面完全静止，逻辑拓扑视图下切换选项也会把动画关掉。

**修复**：动画与视图模式解耦，三种视图（世界地图 / 逻辑拓扑 / 星型拓扑）保持一致。

### 修复 4：星型拓扑残留导致逻辑拓扑画错

**根因**：点击视图按钮时，若之前扫描过局域网，`renderer.lanMode` 仍为 `true`，
渲染器会继续按星型布局画，逻辑拓扑显示成星型（或看起来没显示）。

**修复**：只有"当前展示的确实是局域网星型拓扑"（有拓扑数据且没有探测数据）时才保留星型模式，
否则一律解掉。

### 修复 5：图例面板的毛玻璃导致动画卡顿

**根因**：图例面板使用 `backdrop-filter: blur(8px)`，而它浮在**持续重绘的地图 canvas** 之上，
浏览器每帧都要重新计算背景模糊 —— 这是"地图/拓扑动画卡顿"的经典原因。

**修复**：改用高不透明度实色背景（视觉几乎一致），移除 `backdrop-filter`。

### 实测数据

绘制开销本身很小（单帧 0.12ms：连线 0.04ms + 节点 0.02ms + 标签 0.02ms），
60fps 稳定、帧时间中位数 16.7ms、P95 17.5ms；SVG 标签层重建 10 次/秒（节流生效）。

### 新增测试

| 文件 | 作用 |
| --- | --- |
| `test/topology-perf-e2e.js` | 纯探测逻辑拓扑 / 星型拓扑性能分阶段采样 / 扫描后再探测的逻辑拓扑校验（含"按跳展开"断言与星型残留断言） |

测试入口 `test/run-tests.js` 在 `NS_WITH_BROWSER=1` 下一共运行 9 项，当前全部通过。

---

## 2025-09-17（第七次修订）· 修复逻辑拓扑丢底图 + 逻辑拓扑卡顿

用户反馈：底图选高德后，从「逻辑拓扑」切回「世界地图」仍然显示内置世界地图；
并且逻辑拓扑的动画会卡顿。

### 修复 1：切到逻辑拓扑会永久丢弃用户的底图选择

**根因**：逻辑拓扑视图由内置引擎绘制，原实现直接调用 `switchBaseMap('builtin')`，
而该函数会**永久改写** `state.baseMap = 'builtin'` 并写入 `localStorage`。
于是：

- 用户选择的高德底图被清掉（下拉框的值还显示"高德"，与实际状态不一致）；
- 从逻辑拓扑切回世界地图时，自然只能看到内置地图。

**修复**：新增「挂起 / 恢复」机制，逻辑拓扑期间**临时挂起**高德而不改变用户选择：

- `AmapView.suspend()`：隐藏高德容器与叠加层、停掉动画循环、把渲染目标交还内置画布，
  但**不动 `state.baseMap` 与 `localStorage`**；
- `AmapView.resume()`：重新显示高德、恢复叠加绘制与动画，并按当前拓扑重绘；
- 视图切换只调用这两个方法，底图下拉框与本地存储始终保持用户的选择。

顺带修掉一个同源问题：**局域网扫描后也不再永久丢弃高德选择**——
扫描时记下 `state.pendingBaseMap`，下次探测新目标时自动恢复高德底图并重绘。

### 修复 2：逻辑拓扑动画卡顿

**根因**：`drawOverlay()` 每帧都"清空 + 重建"整个 SVG 标签层
（每个节点一个 `<g><rect><text>`），实测**每秒重建 60 次**（每帧一次），
在 60fps 下相当于每秒上千次 DOM 操作，是卡顿的主因。

**修复**：新增 `shouldRebuildOverlay()` 节流策略：

1. **数据更新、悬停变化、选中变化、尺寸变化**时立即重建（交互不迟钝）；
   通过 `invalidateOverlay()` 在 `setTrace` / `setLanTopology` / `resize` /
   `setHovered` / `setSelected` / `setMode` / `setOptions` 中打脏标记；
2. **平移动画**（数据包流动）期间按节流间隔重建：逻辑拓扑 200ms、地图 100ms，
   既能跟随平移，又不会每帧重建 DOM。

实测效果：SVG 重建次数 **60 次/秒 → 5 次/秒**，绘制帧率仍为 60fps，
帧时间中位数 16.6ms（稳定在一个刷新周期）。

### 说明：为什么逻辑拓扑要用内置引擎

高德是**地理底图**，逻辑拓扑表达的是"链路关系"而非地理位置，
两者在语义上不兼容。因此逻辑拓扑期间挂起高德、改用内置引擎绘制，
但用户的底图选择会被完整保留，切回世界地图即自动恢复。

### 新增测试

| 文件 | 作用 |
| --- | --- |
| `test/view-switch-basemap-e2e.js` | 高德 → 逻辑拓扑 → 世界地图的完整状态校验（底图选择 / localStorage / 下拉框 / 渲染画布 / 叠加层），以及逻辑拓扑的 SVG 重建频率与帧时间 |

测试入口 `test/run-tests.js` 在 `NS_WITH_BROWSER=1` 下一共运行 9 项，当前全部通过。

---

## 2025-09-17（第六次修订）· 修复「扫描局域网后地图与拓扑不显示」

用户反馈：扫描局域网设备后仍显示世界地图、拓扑图不显示；此后再次探测其它目标，
地图与拓扑都不再显示。

### 修复 1：切回内置地图后渲染目标未复原（根因）

`AmapView.destroy()` 只重置了 `Renderer.plain` 标志，**没有把 `renderer.canvas` 换回内置画布**。
高德模式下渲染目标被改成叠加层 `#overlay-canvas`，销毁叠加层时只是把它隐藏并清空，
渲染器却继续往这个隐藏的画布上绘制，于是：

- 内置世界地图上看不到拓扑（画到了隐藏层）；
- 后续探测即使更新了节点/连线数据，屏幕上也毫无变化。

**修复**：在高德视图创建时记住原始渲染目标，`destroy()` 时复原
（`canvas` / `overlay` / `mode` / `plain` / `reproject` 全部复位）；
`switchBaseMap('builtin')` 里再加一道兜底检查，确保渲染器确实回到内置画布。

### 修复 2：局域网扫描后再探测，视图未复位

局域网扫描会把视图切成「逻辑拓扑」（星型拓扑），但这个视图状态在后续探测时没有复位，
于是新探测仍在逻辑拓扑模式下绘制——看起来就像"世界地图不显示"。

**修复**：`resetForNewRun()` 中把视图复位为「世界地图」并同步切换视图按钮；
`renderTraceToActiveView()` 也显式按 `state.view` 设置模式，避免依赖隐式状态。

### 修复 3：局域网扫描后立即绘制，可能画到已隐藏的叠加层

`renderLanTopologyView()` 调用 `switchBaseMap('builtin')` 时没有等待其完成就直接
`renderer.setLanTopology()`，在"当前是高德底图"的情况下会先画到叠加层上。
**修复**：改为 `await switchBaseMap('builtin')`，等底图完全切回后再绘制星型拓扑。

### 新增测试

| 文件 | 作用 |
| --- | --- |
| `test/lan-then-trace-e2e.js` | 四步端到端回归：内置地图探测 → 扫描局域网 → 高德并切回 → 再次探测，逐步校验渲染画布、叠加层状态、陆地像素、拓扑节点/连线/标签 |

测试入口 `test/run-tests.js` 在 `NS_WITH_BROWSER=1` 下一共运行 8 项，当前全部通过。

---

## 2025-09-17（第五次修订）· 完善图例 + 统一两套底图的拓扑样式

### 新增：完整的图例说明面板

工具栏新增「图例说明」按钮，面板**默认展开**（此前用户反馈看不懂红色节点代表什么）。
内容分四组，逐项说明颜色与线条含义：

**节点颜色（角色）**

| 颜色 | 含义 |
| --- | --- |
| 绿色 | 起点 / 本机（带呼吸光晕；本机与网关同址时合并为「本机 / 接入点」） |
| 蓝色 | 中间节点（正常） |
| 粉色 | 目标（带同心环） |
| 灰色 | 完全无响应（三次探测均无回应，显示为 `* * *`） |

**节点颜色（中间节点的异常提示）**

| 颜色 | 含义 |
| --- | --- |
| 黄色 | 轻微异常：丢包 ≥ 20% |
| 红色 | 明显异常：丢包 ≥ 50%，或超时跳，或平均延迟 > 180 ms |

并补充了关键解释：**「超时跳」指该跳自身不回包但后续跳能回应，说明链路是通的**；
红色更多表示"这一段慢或丢包"，而不是断网。

**连线**：按延迟着色（绿→黄→红）、流动光点（延迟越高越慢）、
线上文字（第 N 跳 · 延迟）、虚线/空白（跳过的未定位跳点）。

**局域网拓扑**：网关（黄）、本机（绿）、互联网（粉）、同网段设备（蓝色系按类型区分）。

### 改进：两套底图的节点与连线样式完全统一

**问题**：内置世界地图与高德地图的节点/连线样式不同（一套是手绘 canvas，
一套是 `CircleMarker` / `BezierCurve` / `AMap.Text`），观感不一致。

**方案**：不再"对齐参数"，而是**让高德模式复用内置引擎的绘制代码**：

- 高德只负责画底图（瓦片、路网、中文标注）；
- 节点、连线、标签、动画全部由内置 `Renderer` 绘制到一个透明叠加画布
  （`#overlay-canvas`）上，盖在高德容器之上；
- 通过 `Renderer.plain` 叠加模式跳过底图绘制，并通过 `reproject` 钩子
  在每帧绘制前用 `map.lngLatToContainer()` 刷新节点屏幕坐标，保证平移缩放时拓扑跟随。

因此两个模式的节点半径、呼吸光晕、弧线弧度、数据包动画、标签样式天然一致
（`test/style-consistency-e2e.js` 会逐节点、逐连线比对颜色与标签文本）。

### 修复：叠加层画布空白（切换画布后上下文未更新）

**根因**：切到高德模式时把 `renderer.canvas` 换成了 `#overlay-canvas`，
但 `renderer.ctx` 仍指向原来的 `#map-canvas`——所有绘制都落在被隐藏的画布上，
叠加层始终为空（像素抽样着墨数为 0）。

**修复**：`Renderer.resize()` 检测到画布元素被替换时重新获取 2D 上下文：

```js
if (!this.ctx || this.ctx.canvas !== this.canvas) this.ctx = this.canvas.getContext('2d');
```

### 改进：节点标签直接显示跳数与延迟

节点标签由「名称 + 延迟」升级为「名称 + 第 N 跳 · 延迟」，
例如 `Hong Kong  第 9-10 跳 · 180 ms`。
连线上的文字（`arcLabel`）保持不变，仍显示第 N 跳 / 时延 / 跳过 N 跳。

### 新增测试

| 文件 | 作用 |
| --- | --- |
| `test/style-consistency-e2e.js` | 图例完整性 + 两套底图样式一致性（逐节点/逐连线比对，含叠加层尺寸与遮挡检查） |

测试入口 `test/run-tests.js` 在 `NS_WITH_BROWSER=1` 下一共运行 7 项，当前全部通过。

---

## 2025-09-17（第四次修订）· 修正节点着色、高德跳数标签、切回内置地图失效

### 修复：中间节点被染成绿色（着色规则重写）

**现象**：部分中间节点显示为绿色，与图例（中间节点=蓝色）不一致，分不清哪个是起点。

**根因**（`public/js/draw.js` 的 `nodeColor()`）：两条规则把"起点绿"过度扩散到了中间节点：

1. `node.isStart` —— 当本机与网关落在同一坐标时两者会合并成一个节点并标记为起点，
   该规则会让这个合并节点一律显示绿色（但它同时也是"第 1 跳"）；
2. `geoStatus === 'private'` —— 任何私网地址的跳都用起点色，而运营商内部大量使用
   10.x / 100.64.x 地址，这些中间跳被误染成绿色。

**更严重的一处误判**：时延规则排在前面并直接返回绿色，
于是**丢包 100% 的跳因为时延数值很小而被显示成绿色**——把"不通"显示成了"健康"。

**修复后的规则**（角色定色 + 异常标记）：

| 角色/状态 | 颜色 |
| --- | --- |
| 起点 / 本机 | 绿色（呼吸光晕） |
| 目标 | 粉色（带同心环） |
| 中间节点（正常） | **本色蓝** |
| 中间节点 · 丢包 ≥ 20% | 黄色 |
| 中间节点 · 丢包 ≥ 50% / 超时跳 / 时延 > 180ms | 红色 |
| 完全无响应（`* * *`） | 灰色 |

高德渲染分支的 `colorFor()` 同步为同一套规则。工具栏图例也补充了「异常跳转黄/红」说明。

### 修复：高德地图模式下看不到连线跳数

**根因**：高德渲染分支只创建了 `AMap.BezierCurve` 画线，
没有像内置引擎的 `arcLabel()` 那样生成文字标签。

**修复**：为每条连线生成 `AMap.Text` 标签，内容为「第 N 跳 · 时延」，
样式与内置引擎保持一致（深底、彩边、等宽字体）；节点上也补齐了 `ttl` / `lossPct` 字段。

### 修复：从高德切回内置世界地图后地图消失（严重）

**现象**：切到高德后再切回内置地图，地图区域完全空白，且内置地图再也画不出来。

**根因**：高德地图的容器直接用了 `#canvas-wrap`，而**高德的 `destroy()` 会清空容器内容**——
于是 `<canvas>`、SVG 标签层、统计栏被一并删除；紧接着
`Renderer.resize()` 读取已被移除的 `canvas.parentElement.getBoundingClientRect()`
抛出 `TypeError: Cannot read properties of null`，切换流程中断。

**修复**：

1. 新增专用容器 `#amap-host`（绝对定位铺满画布区），高德只挂在这里，
   `destroy()` 只影响自身；切换时同步清空其内容并隐藏；
2. `Renderer.resize()` 增加防御：容器或画布不存在时直接返回而不是抛错；
3. 切回内置地图时强制重新量取尺寸并重绘当前数据（拓扑 / 星型拓扑）。

**回归测试**还会断言「画布中心最上层元素就是内置画布」，防止高德残留覆盖导致无法交互。

### 顺带改进

- 内置地图的标签显示门槛从 4 个节点放宽到 8 个，更多场景下可直接看到节点名称；
- 工具栏图例补充「异常跳转黄/红」的说明与悬浮提示。

### 新增测试

| 文件 | 作用 |
| --- | --- |
| `test/node-color-check.js` | 13 种角色组合的着色规则断言（直接在浏览器里调用 `nodeColor`） |
| `test/map-switch-e2e.js` | 三项问题的端到端验收：着色明细、高德跳数标签、切回内置地图（含遮挡检测） |

测试入口 `test/run-tests.js` 在 `NS_WITH_BROWSER=1` 下一共运行 6 项，当前全部通过。

> 说明：高德用 WebGL 渲染，`canvas.getImageData()` 在未开启 `preserveDrawingBuffer` 时读到空白，
> 因此高德验收改为对 CDP 截图做 PNG 解码 + 像素分析（与渲染方式无关，更可靠）。

---

## 2025-09-17（第三次修订）· 高德地图底图 + 局域网设备发现

### 新增：高德地图（AMap）底图，可与内置世界地图一键切换

- **工具栏新增「底图」选择器**：内置世界地图（全球覆盖，跨洲路由完整）/ 高德地图（国内地理信息更细）。
  选择记在 `localStorage`，下次打开自动恢复。
- **服务端代理**（`src/core/amap.js` + `/api/amap/*`）：高德自 2021-12-02 起要求携带安全密钥，
  官方推荐由服务端拼接受保护请求。因此安全密钥只存在于服务端，页面源码中看不到。
- **设置面板**（左侧「⚙ 地图设置」）：填写 Key 与安全密钥、一键「测试连接」、
  保存到 `data/amap-config.json`（已加入 `.gitignore`）。
  「测试连接」会分别检测 Key 格式、安全密钥、JS API 脚本、Web 服务接口，并逐项给出结论与处理建议。
- **错误码中文对照**：`INVALID_USER_KEY`、`INVALID_USER_SCODE`、`USERKEY_PLAT_NOMATCH`、
  `INVALID_USER_DOMAIN`、`INVALID_USER_IP`、`DAILY_QUERY_OVER_LIMIT` 等均有明确说明。
- **未配置 Key 时自动回退**到内置世界地图，功能不受影响，状态栏给出提示，不出现空白页。
- 覆盖物全部用高德原生对象绘制：`CircleMarker`（节点）、`BezierCurve`（路由弧）、
  `Polyline`（局域网连线），标签层复用同一套 SVG 逻辑。

**实测结论（用真实 Key 验证）**：JS API 脚本加载成功（965 KB），地图容器创建，
瓦片接口 `jsapi.amap.com/web_map/get_tile` 全部返回 200，截图中可见上海/杭州/苏州及各区县中文标注。
该 Key 为「Web端(JS API)」类型，调用 Web 服务接口会返回 `USERKEY_PLAT_NOMATCH`（10009），
这属预期情况——底图完全可用，仅服务端 IP 定位需要另外的「Web服务」Key，程序会明确提示。

### 新增：局域网设备发现与星型拓扑图

点「扫描局域网设备」后：

1. 读取本机网卡/掩码/网关/DNS；
2. 对所在 /24 网段做 TCP 存活探测（最多 512 地址、并发 64、单地址 800ms）；
3. 读取 ARP 邻居表（**新增过滤多播组地址**，见下）；
4. 反向 DNS 解析主机名；
5. SSDP（UPnP）组播发现，获取设备描述；
6. mDNS 查询常见服务类型；
7. 内置 OUI 表（919 条）识别 MAC 厂商；
8. 综合厂商/主机名/SSDP 推断设备类型（电脑/手机/路由器/打印机/NAS/摄像头/电视/IoT/虚拟网卡）。

**拓扑呈现**：局域网设备使用私有地址、在世界地图上没有真实经纬度，因此用**星型拓扑**
表达连接关系——网关居中、设备环绕、本机在上、互联网在下，每个节点标注局域网地址与主机名，
颜色按设备类型区分。若此时启用了高德底图，会自动切回内置引擎并说明原因。

### 修复：多播地址被当作设备

`arp -a` 输出中的 `224.0.0.x`、`239.255.255.250`（IGMP / SSDP 组播）会被当成真实设备，
在拓扑图里画出不存在的节点。现在 `parseArp()` 统一过滤 `224.0.0.0/4`，
并新增单元测试锁定该行为（单元用例数 100 → 101）。

### 修复：局部管理 MAC 无法识别厂商

`3E:0A:04:xx` 这类 MAC 的第二位最低有效位为 1，属于**本地管理地址**
（虚拟机、随机化 MAC、Mesh 节点），不会出现在 IEEE 分配表中。
现在会明确标注「本地管理地址（虚拟机/随机 MAC）」，而不是笼统写「未知设备」。

### 修复：`state.target` 被赋成字符串导致追踪中断

`Cannot create property 'primaryIP' on string` —— `runTrace()` 时 `state.target` 只被设为目标字符串，
`done` 事件里对其赋属性会抛 TypeError，导致追踪结果丢失。现在统一归一化为对象。

### 修复：局域网拓扑节点贴边、标签被裁

星型布局半径未预留标签空间，窗口较小时节点落在画布边缘、标签被裁掉。
现在半径按画布尺寸推导并强制保留边距（横向 ≥78px、纵向 ≥64px），
坐标再做一次钳制；星型模式下所有标签强制显示。

### 修复：`renderLanPanel is not defined`

`runLanScan()` 调用了一个已不存在的函数，导致扫描成功后抛错、拓扑不显示
（而错误 toast 很快消失，表现为"扫描没反应"）。现已改为复用 `renderLocalPanel()`
并新增 `buildLocalPanelInput()` 做数据适配。

### 新增测试

| 文件 | 作用 |
| --- | --- |
| `test/lan-topology-e2e.js` | 局域网拓扑验收：真实扫描 + 星型拓扑断言 + 截图 |
| `test/amap-e2e.js` | 高德底图验收：脚本/实例/瓦片/像素抽样/截图，未配置 Key 自动跳过 |
| `test/run-tests.js` | 汇总入口支持 `NS_WITH_BROWSER=1` 纳入两项浏览器验收 |

当前测试规模：单元 101 用例、接口冒烟 12 项、浏览器 E2E、渲染审计、布局与投影验收、
局域网拓扑验收、高德底图验收、前端静态校验。

---

## 2025-09-17（第二次修订）· 投影等比例化、统计栏重做

### 修复：地图被纵向拉升 24%（真正的"拉升"根因）

**现象**：大陆看起来被纵向拉长、横向压扁，"整体被拉伸"。

**根因**：等距圆柱投影只有在 **x/y 方向比例尺相同**时才是"等距"的，而画布高度取了
整数 720px：

| 方向 | 跨度 | 像素 | 比例尺 |
| --- | --- | --- | --- |
| 经度 | 360° | 1440px | **4.000 px/度** |
| 纬度 | 145°（-60~85） | 720px | **4.966 px/度** |

纵向比例尺比横向大 24.1%，所以地图被纵向拉伸了近四分之一。

**修复**（`tools/build-world-map.js`）：画布高度不再写死，而是由等比例推导

```js
const HEIGHT = Math.round((WIDTH / (LON_MAX - LON_MIN)) * (LAT_MAX - LAT_MIN)); // 580
```

结果：**经度 4.000 px/度、纬度 4.000 px/度，比值 1.00000**，地图不再变形；
数据文件同时从 147 KB 降到 121 KB。输出中还新增 `pxPerDegree` 与 `aspectRatio`
两个字段，供前端与测试直接校验（`test/layout-e2e.js` 会断言比值必须为 1）。

### 重新设计：底部统计栏

**旧问题**：目标项与 KPI 项挤在同一行网格里，地址一长就挤压相邻项（"遮挡跳数"），
而且只是把目标列加宽，属于治标。

**新设计**：统计栏改为分两行的自适应网格

- **整条与画布左右对齐**：`left: 14px; right: 14px`，宽度随窗口自适应，左右间距恒等；
- **目标行独占整行**：地址再长也只撑宽自己这一行，靠 `overflow: hidden` +
  `text-overflow: ellipsis` 自适应（放得下就完整显示，放不下就省略号），
  完整值通过 `title` 悬浮查看；**永远不会挤压下方的 KPI**；
- **KPI 行平分整行**：6 列等宽网格铺满，天然左右对齐（实测铺满度 98%）；
- **窄窗口（<900px）自动两列**，仍然左右对齐；
- 目标行的值用强调色，并带下分隔线与 KPI 行区分。

回归测试（`test/render-audit.js`）注入超长 IPv6 与超长域名，断言：
不溢出容器、项目零重叠、左右间距相等（±1px）、KPI 行铺满度 ≥95%。

### 新增：`test/layout-e2e.js`

真实探测一次跨洲目标，验收：地图等比例（px/度比值 = 1）、统计栏左右对齐、
目标地址不溢出、连线不跨越未定位节点、页面无 console 错误，并保存
`data/screenshots/final-map.png` 与 `final-map-long-target.png` 两张对照图。

### 关于"横条纹"的复核结论

用像素级扫描复核（逐行统计陆地覆盖率、孤立尖峰、横贯全宽细线）：
**当前版本三项指标均为 0**，覆盖率曲线平滑，横条纹确已消除。
如果仍然看到条纹，请强制刷新页面（Ctrl+F5）——浏览器可能缓存了修复前的地图数据；
服务端已对 `public/data/*` 与 HTML 关闭强缓存，正常刷新即可拿到新数据。

---

## 2025-09-17（第一次修订）· 地图显示修复与连线规则优化

### 修复：北半球出现横条纹（重要）

**现象**：世界地图北半球出现横贯整幅画布的深色带子，看起来像被"压缩"或"扫描线"。

**根因**：数据源（Natural Earth TopoJSON）中，跨越 ±180° 日界线的多边形
（俄罗斯、斐济、南极洲）经过等距圆柱投影后，**同一个环内同时存在 x≈0 与 x≈1440 的点**，
闭环填充时就画出了一条横贯全图的带子。此外南极洲存在一条零高度退化环，在地图底边画出横线。

这里有个隐蔽之处：源数据已经把经度规范化到 `[-180, 180]`，所以跨越日界线的边会表现为
`178.6 → -180.0` 这种"小跳变"（最短路径只有 1.4°），**用"相邻经度差 > 180"根本检测不出来**。

**修复**（`tools/build-world-map.js`，构建期处理）：

1. **展开经度**：逐点累加最短经度差，把折线展开成连续经度；
2. **按边界拆分**：以 ±180° 为界，把跨界的环拆成"贴左边界"和"贴右边界"两部分；
   直接闭合跨界的环会得到一条横贯全图的边，必须拆开；
3. **裁剪**：把每部分裁剪到 `[-180, 180]`。
   这里特意没有使用 Sutherland–Hodgman —— 当多边形横跨整个条带时，
   该算法会把"跨越的一侧"压缩成一条退化细缝，正是要消除的横条纹；
   改用"按交点切段 + 沿边界闭合"的做法；
4. **丢弃退化碎片**：过滤掉包围盒不足 0.5 像素或面积趋近于零的线状环。

**结果**：176 个国家/地区、275 个环，**0 个横贯环、0 个退化环**。
代价是南极洲被移除——本投影纬度下限为 -60°，南极洲原本只在地图底边留下一条零高度线，
移除它是正确的（北极圈、格陵兰、西伯利亚等高纬度区域均完整保留）。

### 优化：地图连线只连接能定位到的节点

**需求**：如果某一跳无法确定地理位置，地图上的连线不应穿过它。

**实现**：`public/js/draw.js` 的 `setTrace()` 只把**能确定地理位置的跳点**纳入拓扑节点，
未定位的跳点单独记录在 `renderer.unlocated` 中：

- 地图上不画点、不连线，表现为一段空白；
- 相邻两个已定位节点之间**只连这两点**，并在连线上标注"跳过 N 跳"；
- 统计栏新增「已定位」「未定位」两个计数，未定位不为 0 时以警示色提示；
- 跳点表格中未定位的行明确标注原因（未响应 / 无地理定位数据），
  点击时弹出说明卡片而不是假装定位成功；
- 连线着色改为取"最近一个有效延迟"，避免某一跳超时导致后续所有连线变灰。

### 修复：视图拖出视野后无法恢复

持续拖动或缩放会把地图完全移出视野，用户面对空白画布且无法自行恢复（只能刷新页面）。
现在 `panBy()` / `zoomAt()` 均会调用 `clampView()`，保证地图边缘最多移动到距画布边缘 50% 处。

### 修复：窄窗口下控制面板被隐藏且无入口

窗口宽度小于 900px 时左侧控制面板会被 CSS 隐藏，但没有任何展开入口，导致无法操作。
现在顶栏提供两个按钮：

- `☰` 展开/收起左侧控制面板（带遮罩，点击遮罩关闭）；
- `▤` 展开/收起右侧数据面板，面板内也有 `✕` 收起按钮。

### 修复：统计栏中过长的目标地址遮挡跳数

目标地址（超长 IPv6、复杂域名）会撑破统计栏布局，压住相邻的「跳数」。
现在统计栏改为固定列宽网格，「目标」独占两列并启用省略号截断，完整信息通过 `title` 悬浮查看。
回归测试会注入超长 IPv6 与超长域名验证不重叠、已截断。

### 修复：地图数据与前端资源的缓存策略

`public/data/*` 原本使用 `max-age=300` 强缓存，地图数据更新后浏览器仍显示旧图（继续看到横条纹）。
现在数据目录统一 `no-cache`（仍允许 304 协商缓存），HTML 亦然。

### 修复：解析与解析器的若干缺陷

由测试套件独立发现的缺陷，均已修复并转为回归用例：

| 缺陷 | 影响 |
| --- | --- |
| UTF-8 中文被误判为 latin1 | 中文输出变乱码 |
| 中文 `ipconfig` 网卡段落永不匹配 | 网关/DNS 解析为空 |
| netstat 状态列取错 | 所有 TCP 监听端口被丢弃 |
| Windows 英文 ping 统计行未解析 | 丢包链路被误判为健康 |
| `time=<1ms` 形式丢失 | 少数延迟样本缺失 |
| `Reply from 8.8.8.8:` 取不到 IP | 反向解析结果为 null |
| 中文 arp「动态」被标为静态 | 邻居表类型显示错误 |
| 每跳探测次数硬编码 | 出现 `-200%` 的负丢包率 |

### 新增测试

| 文件 | 作用 |
| --- | --- |
| `test/render-audit.js` | 地图渲染对抗性审计（合成数据 + 像素抽样 + 布局检查） |
| `test/validate-frontend.js` | 前端静态校验（DOM id、标签闭合、脚本顺序、资源存在性） |
| `tools/recover-encoding.js` | 编码损坏恢复工具（自建 GBK 反向编码表） |
| `tools/publish-github.js` | 发布到 GitHub 的辅助脚本（含上传前检查） |
